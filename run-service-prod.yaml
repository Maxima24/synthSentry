apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: synthsentry-web-prod
spec:
  template:
    metadata:
      annotations:
        run.googleapis.com/execution-environment: gen2
        autoscaling.knative.dev/minScale: "1"
        autoscaling.knative.dev/maxScale: "10"
    spec:
      containers:
        - image: synthsentry-web
          ports:
            - containerPort: 8080
          env:
            - name: NODE_ENV
              value: production